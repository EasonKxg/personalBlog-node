const pool = require("../../app/pool");

class articleService {
  async list() {
    const text = ``;
  }

  async insert(req) {
    console.log(req);
    const text = ``;
  }
}

module.exports = new articleService();
